<control:WindowBase x:Class="CatchingFire.Area.Sys.View.LogPreview"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:control="clr-namespace:UtilityControl.Control;assembly=UtilityControl"
        Title="Log" TitleHorizontalAlignment="Center" IconVisibility="Collapsed" CaptionBackground="White" CaptionForeground="#737373" MaxboxEnable="False" Height="500" Width="900" BorderBrush="#ffffff" >
    <Grid>
        <FlowDocumentScrollViewer HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Auto">
            <FlowDocumentScrollViewer.Document>
                <FlowDocument>
                    <Section>
                        <Paragraph>
                            <Run Text="{Binding Value}"></Run>
                        </Paragraph>
                    </Section>
                </FlowDocument>
            </FlowDocumentScrollViewer.Document>
        </FlowDocumentScrollViewer>
        <!--<ScrollViewer HorizontalAlignment="Stretch" VerticalAlignment="Stretch" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" >
            <Grid>
            <TextBlock Margin="5" VerticalAlignment="Top" HorizontalAlignment="Left" TextWrapping="WrapWithOverflow"  Text="{Binding Value}"></TextBlock>
            </Grid>
        </ScrollViewer>-->
    </Grid>
</control:WindowBase>
